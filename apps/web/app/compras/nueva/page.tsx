import { EditorCompra } from '@/components/compras/editor-compra';

/** Nueva factura de compra con retención de IVA/ISLR en el mismo flujo (doc 06 M2). */
export default function NuevaCompraPage() {
  return <EditorCompra />;
}
